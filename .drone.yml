---
kind: pipeline
type: kubernetes
name: backend

steps:
- name: chown
  image: rustlang/rust:nightly-slim
  commands:
    - ls -la /drone/src
    - whoami
    - "chown $(whoami):$(whoami) -R /drone/src"

- name: test
  image: rustlang/rust:nightly-slim
  commands:
  - cargo test
